<p id="notice"><%= notice %></p>
<div class="col-md-12">
<h1>Listing Products</h1>
Всего товаров <%=@products.count%>
</div>
<div class="col-md-12">
<%= link_to 'Загрузить товары', downloadproduct_products_path%>| <%= link_to 'Обновить товары', updateproduct_products_path%> | Синхронизировать кол-во и цены
</div>
<div class="col-md-12">
	<div class="media">
	  <div class="media-body">
			<table class="table table-hover">
			  <thead>
			    <tr>
			      <th>Inid</th>
			      <th>Sku</th>
			      <th>Title</th>
			      <th>Qt</th>
			      <th>Cost price</th>
			      <th>Price</th>
			      <th colspan="3"></th>
			    </tr>
			  </thead>
			
			  <tbody>
			    <% @products.each do |product| %>
			      <tr>
			        <td><%= product.inid %></td>
			        <td><%= product.sku %></td>
			        <td><%= product.title %></td>
			        <td><%= product.qt %></td>
			        <td><%= product.cost_price %></td>
			        <td><%= product.price %></td>
			        <td><%= link_to 'Show', product %></td>
			        <td><%= link_to 'Edit', edit_product_path(product) %></td>
			        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
			      </tr>
			    <% end %>
			  </tbody>
			</table>
<%= will_paginate @products %>
	  </div>
	</div>
</div>

